# Miles-per-Gallon

total_distance_travel = float(input("Total travel in miles: "))

total_gas_gallon = float(input("Total no. of gas gallons: "))

mpg = total_distance_travel / total_gas_gallon

print("The car's MPG is", mpg)